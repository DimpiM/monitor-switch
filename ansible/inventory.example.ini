# Copy to inventory.ini and edit. inventory.ini is gitignored.
[monitorctl]
my-pi ansible_host=monitorswitch.local ansible_user=pi

[monitorctl:vars]
# Uncomment if your SSH key is not the default one.
# ansible_ssh_private_key_file=~/.ssh/id_ed25519_my_pi
