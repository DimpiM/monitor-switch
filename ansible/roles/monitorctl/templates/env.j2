# Managed by Ansible — local edits will be overwritten.
# Mode 0640, readable only by root and the service group.
{% if monitorctl_mqtt_enabled and monitorctl_mqtt_username %}
MONITORCTL_MQTT_USERNAME={{ monitorctl_mqtt_username }}
MONITORCTL_MQTT_PASSWORD={{ monitorctl_mqtt_password }}
{% else %}
# No MQTT credentials configured.
{% endif %}
