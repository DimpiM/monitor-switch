[defaults]
inventory = inventory.ini
roles_path = roles
host_key_checking = True

# A Pi Zero 2 W is slow; the health check at the end waits on a real service.
timeout = 30

[ssh_connection]
pipelining = True
